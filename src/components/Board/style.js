import styled from 'styled-components'

export const BoardContainer = styled.div`
   display: flex;
   padding: 50px 0;
   flex-wrap: wrap;
   gap: 20px;
   justify-content: center;
   position: relative;
`
